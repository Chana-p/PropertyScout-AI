services:
  - type: web
    name: propertyscout-frontend
    runtime: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: npm install -g serve && serve -s frontend/dist -l 5173
    envVars:
      - key: NODE_VERSION
        value: 24.14.1
